/**
 * The character representing an empty tape cell.
 */
export const EMPTY = "⬚";

/**
 * The number of steps to run before stopping automatically.
 */
export const DEFAULT_STEP_LIMIT = 1_000_000;

/**
 * The default content of the editor when creating a new file.
 */
export const DEFAULT_DOC = `% Write your transition rules here!
`;
